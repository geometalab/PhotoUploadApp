class UploadService {
  void uploadImage(var image, String title, String description, String author,
      String license, DateTime date) {
    throw UnimplementedError();
    // TODO Implement uploading (needs api key)
  }
}
